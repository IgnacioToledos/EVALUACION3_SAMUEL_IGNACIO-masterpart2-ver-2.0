import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.registr.app',
  appName: 'Aplicacion_mov_RegistrAPP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
